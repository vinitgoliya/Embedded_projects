I2C:
----
-> Inter Integrated Circuit
-> Serial Communication
-> Synchronous Communication Protocol
-> Half Duplex Communication
-> Invented by Philips (Netherland)

-> 2 Wired Communication
   -> SCL - Clock line
   -> SDA - Data Line

-> Communication between Master and Slave
  
   Master : Microcontroller

   Slave: A device being addressed by the master is cladded slave
   LCD
   RTC
   EEPROM  (EnergyMeter)
   ADC
   SSD (seven segment Display)
   RFID

-> Select the address: To select the slave using 7 bit slave address.

-> How many slave devices you can connect at a time:
   depends on slave address bit
   slave - 7 bit address
   2 to the power 7 -> 128 slave devices (different slave devices)

   Same slave devices -> 8 devices


-> less Faster that SPI Communication --> For longer distance

-> Baud rate:
   Standard Mode -> 100Kbps
   Full speed Mode -> 400Kbps
   Fast Mode -> 1 Mbps
   High Speed Mode -> 3.4Mbps


Data Frame:
----------
-> Start Signal
-> Slave Adress and R/W bit Transmission
-> Data transfer
-> Stop Signal


Writing on I2C Bus:  (Master sending data to Slave)
-----------------------------------------------------




Reading from I2C Bus:  (Master will read data from Slave)
-----------------------------------------------------




Clock Stretching:
---------------




Bit Arbitration:
------------------


LPC2148 I2C:
-----------
I2C (Inter Integrated Circuit) is a serial bus interface connection protocol first invented by Philips Semiconductor (Now NXP Semiconductors). It is also called as TWI (two wire interface) since it uses only two wires for communication. I2C uses a handshaking mechanism for communication. Hence, it is also called as acknowledgment based communication protocol.

I2C works in two modes namely,

Master Mode
       Master is responsible for generating clock and initiating communication
Slave Mode
       Slave receives the clock and responds when addressed by the Master
Two wires of the I2C interface are SDA (serial data) and SCL (serial clock).

SDA is Serial Data wire used for data transfer in between master and slave
SCL is Serial Clock wire used for clock synchronization. Clock is provided by the master
There are two types of data transfer possible on I2C interface depending on Read/Write operation i.e.

Data transfer from Master Transmitter to Slave Receiver and

Data transfer from Slave Transmitter to Master Receiver


->LPC2148 has two I2C interfaces. i.e. I2C0 & I2C1
->It has Programmable clock to support adjustable I2C transfer rates
->LPC2148 I2C is byte oriented and has four operating modes:
	Master Transmitter mode
	Master Receiver mode
	Slave Transmitter mode
	Slave Receiver mode

I2C0
-----
Pin No - 22 : SCL0
Pin No - 26 : SDA0

I2C1:
----
Pin No - 37 : SCL1
Pin No - 41 : SDA1

SCL0: - Serial Clock pin of I2C0.
SDA0: - Serial Data pin of I2C0.
SCL1: - Serial Clock pin of I2C1.
SDA1: - Serial Data pin of I2C1


Pin configuration
Clock set
Data Frame
Accownledgement
Start Bit
Stop Bit
Slave (slave address + Read Write operation)
Sending Data
Receiving data

-----------------------------------------------------------------------------
I2C Driver:
---------
1. Acknowledgement Bit:
-----------------------
I2C0CONSET
Bit 2 – AA (Assert Acknowledge Flag)
When set to 1, Acknowledge (SDA ---> LOW) is returned during acknowledge clock pulse on SCL. Otherwise, Not acknowledge (SDA --> HIGH) is returned.

#define ASSERT_ACK_BIT()        I2C0CONSET |= (1<<BIT2)

I2C0CONCLR
Bit 2 – AAC (Assert Acknowledge Flag Clear)
Setting this bit clears the AA bit in I2C0CONSET register.

#define ACCERT_NACK_BIT()    I2CCONCLR |= (1<<BIT2)


2. Enable/Disable Start Bit:
-----------------------------
I2C0CONSET:
----------
Bit 5 – STA (Start Flag)
Setting this bit causes the I2C interface to enter in master mode and transmit a START condition or transmit a repeated START condition if it is already in master mode.

I2C0CONCLR:
----------
Bit 5 – STAC (Start Flag Clear)
Setting this bit clears the STA bit in I2C0CONSET register.

#define ENABLE_START_BIT()    I2C0CONSET |= (1<<BIT5)
#define DISABLE_START_BIT()   I2CCONCLR |= (1<<BIT5)


3. Enable Stop Bit:
-----------------------------
I2C0CONSET:
----------
Bit 4 – STO (Stop Flag)
In Master Mode, setting this bit causes the I2C interface to transmit a STOP condition. In Slave Mode, setting this bit causes recover from an error condition if any.

#define ENABLE_STOP_BIT()    I2C0CONSET |= (1<<BIT4)


4. Enable/Disable I2C Interface:
------------------------------
I2C0CONSET:
----------
Bit 6 – I2CEN (I2C Interface Enable)
Set this bit to enable I2C interface.

I2C0CONCLR:
Bit 6 – I2CENC (I2C Interface Disable)
Setting this bit clears the I2CEN bit in I2C0CONSET register.


#define ENABLE_I2C_BIT()    I2C0CONSET |= (1<<BIT6)
#define DISABLE_I2C_BIT()   I2C0CONCLR |= (1<<BIT6)


5. I2C State change or Data Pass:
--------------------------------
I2C0CONSET
Bit 3 – SI (I2C Interrupt Flag)
This bit is set when the I2C state changes. Else it remains reset.
I2C0CONCLR
Bit 3 – SIC (I2C Interrupt Flag Clear)
Setting this bit clears the SI bit in I2C0CONSET register

#define CLR_SI_BIT()   I2C0CONCLR |= (1<<BIT6)
  

6. I2C0 Pin Configuration:
-------------------------
PINSEL0.P0_2 = P0_2_SCL0;
PINSEL0.P0_3 = P0_3_SDA0;

7. Wait for I2C bus Status:
--------------------------
void Wait_Fot_status()
{
CLR_SI_BIT();    // data is passing
while(!(I2C0CONSET & (1<<BIT3)));     // Wait for I2C bus busy
}

8. Initialization of I2C0
---------------------------

- I2C0 Pin Configuration
- Power_ON_I2C0();    ---> PCONP
- To set the clock
----------------
I2C0SCLL - It will consist low cycle of SCL
I2C0SCLH - It will consist High cycle of SCL

I2C-> For setting clock we will consider 50% duty cycle
Ton -> 50%   Toff-50%


I2c Frequency = FpCLK/I2C0SCLL + I2C0SCLH

Clock Frequency = 100 Khz
Fpclk = 60 MHz

I2C0SCLL = 300;
I2C0SCLH = 300;

- Enable of I2C0
ENABLE_I2C_BIT();

9. Start_I2c0
-------------
-> Send the start condition
-> Wait for I2C bus Status
- Disable the start Bit
-> Send slave address
-> Wait for status

void Strat_I2C0(unsigned char slave_adress)
{
  ENABLE_START_BIT();
  Wait_Fot_status();
  DISABLE_START_BIT();
  I2C0DAT = slave_adress;
  Wait_Fot_status();
}


10. Send I2C0 Data:
-----------------
void Set_I2C0_data(unsigned char DAT)
{
 I2C0DAT = DAT;
 Wait_Fot_status();
}

11. Send STOP condition:
------------------------
CLR_SI_BIT();
ENABLE_STOP_BIT();

----------------------------------------------------------------------------

Receive:
------




































































































