C51 COMPILER V9.60.7.0   MAIN                                                              03/20/2025 09:49:18 PAGE 1   


C51 COMPILER V9.60.7.0, COMPILATION OF MODULE MAIN
OBJECT MODULE PLACED IN ..\bin\main.obj
COMPILER INVOKED BY: C:\Users\user\embedded_development\C51\BIN\C51.EXE ..\app\main.c OPTIMIZE(0,SPEED) BROWSE INCDIR(..
                    -\include) DEBUG OBJECTEXTEND PRINT(.\Listings\main.lst) TABS(2) OBJECT(..\bin\main.obj)

line level    source

   1          #include<reg51.h>
   2          #include<timer.h>
   3          #include<lcd.h>
   4          #include<string.h>
   5          
   6          
   7          void printName(char *str)
   8            {
   9   1          Set_LCD_Command(0x80); //Force cursor to the beginning ( 1st line)
  10   1          Set_LCD_String(str);
  11   1          //Set_LCD_Char('A');
  12   1          Set_LCD_Command(0xC0); // Force cursor to the beginning ( 2nd line)
  13   1          Set_LCD_String("Goliys");
  14   1        //  Set_LCD_Char('B');
  15   1        }
  16            
  17          
  18            
  19          void intToStr(int num, char *str) {
  20   1          int i = 0, j, rem;
  21   1          char temp;
  22   1      
  23   1          // Extract digits and store them in reverse order
  24   1          do {
  25   2              rem = num % 10;      // Get the last digit
  26   2              str[i++] = rem + '0'; // Convert digit to ASCII
  27   2              num /= 10;           // Remove the last digit
  28   2          } while (num > 0);
  29   1      
  30   1          str[i] = '\0'; // Null-terminate the string
  31   1      
  32   1          // Reverse the string
  33   1          for (j = 0; j < i / 2; j++) {
  34   2              temp = str[j];
  35   2              str[j] = str[i - j - 1];
  36   2              str[i - j - 1] = temp;
  37   2          }
  38   1      }
  39          
  40          void formatTime(int hours, int minutes, int seconds, char *timeStr) {
  41   1          char temp[3]; // Temporary buffer for each component
  42   1      
  43   1          // Format hours
  44   1          if (hours < 10) {
  45   2              temp[0] = '0';         // Add leading zero
  46   2              temp[1] = hours + '0'; // Convert to ASCII
  47   2              temp[2] = '\0';
  48   2          } else {
  49   2              temp[0] = (hours / 10) + '0';  // Tens place
  50   2              temp[1] = (hours % 10) + '0';  // Units place
  51   2              temp[2] = '\0';
  52   2          }
  53   1          strcpy(timeStr, temp);     // Copy hours to the result string
  54   1          strcat(timeStr, ":");      // Add colon separator
C51 COMPILER V9.60.7.0   MAIN                                                              03/20/2025 09:49:18 PAGE 2   

  55   1      
  56   1          // Format minutes
  57   1          if (minutes < 10) {
  58   2              temp[0] = '0';
  59   2              temp[1] = minutes + '0';
  60   2              temp[2] = '\0';
  61   2          } else {
  62   2              temp[0] = (minutes / 10) + '0';
  63   2              temp[1] = (minutes % 10) + '0';
  64   2              temp[2] = '\0';
  65   2          }
  66   1          strcat(timeStr, temp);     // Add minutes to the result string
  67   1          strcat(timeStr, ":");      // Add colon separator
  68   1      
  69   1          // Format seconds
  70   1          if (seconds < 10) {
  71   2              temp[0] = '0';
  72   2              temp[1] = seconds + '0';
  73   2              temp[2] = '\0';
  74   2          } else {
  75   2              temp[0] = (seconds / 10) + '0';
  76   2              temp[1] = (seconds % 10) + '0';
  77   2              temp[2] = '\0';
  78   2          }
  79   1          strcat(timeStr, temp);     // Add seconds to the result string
  80   1      }
  81          
  82          
  83          
  84          
  85          
  86          void main()
  87            {
  88   1          int seconds = 0;
  89   1          char strSeconds[6];
  90   1          Init_LCD();
  91   1          config_25msec();
  92   1          printName(strSeconds);
  93   1          while(1)
  94   1            {
  95   2      
  96   2      
  97   2            
  98   2      
  99   2            overflow_check();
 100   2            if(counter==40)
 101   2              {
 102   3              //  strcpy(str, "125");
 103   3                intToStr(seconds, strSeconds);// working well
 104   3              //  printName(strSeconds);
 105   3                //formatTime(hours, minutes, seconds, timeStr);
 106   3                //formatTime(11, 03, 42, strSeconds);
 107   3                printName(strSeconds);
 108   3                if(seconds == 60)
 109   3                  {
 110   4                    seconds = 0;
 111   4                  }
 112   3                counter = 0;
 113   3                seconds++;
 114   3                P1 = ~P1;
 115   3              }
 116   2            }
C51 COMPILER V9.60.7.0   MAIN                                                              03/20/2025 09:49:18 PAGE 3   

 117   1        }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =    735    ----
   CONSTANT SIZE    =      9    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =     35    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
